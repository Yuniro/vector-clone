/* eslint-disable no-shadow */
type ModelPool = {
  name: string;
  description?: string;
  icon?: string;
  background: string;
  increment: number;
  deposit: number;
  tvlValue: string;
  claimable: string;
};

export default ModelPool;
